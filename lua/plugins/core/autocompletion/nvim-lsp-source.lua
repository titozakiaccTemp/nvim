-- source for : nvim-cmp source for neovim's built-in language server client. you can get fitur like auto import on complete if lsp support it, moving between snippets, expanding snippets
-- nvim-cmp source for neovim's built-in language server client.
return {
	"hrsh7th/cmp-nvim-lsp",
}
